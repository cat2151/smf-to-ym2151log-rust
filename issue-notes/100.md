# issue 添付JSONのdemoに、ビブラート、ポルタメント、音色レジスタへのソフトウェアLFO、のdemoが含まれてか、code机上チェックする #100
[issues #100](https://github.com/cat2151/smf-to-ym2151log-rust/issues/100)

## 机上確認メモ
- 添付 JSON を使ったデモは `demo-library/` 配下に3ページ存在：`index.html`（任意の添付 JSON をアップロードできる汎用デモ）、`delay-vibrato.html`（デフォルト添付に `{"DelayVibrato": true}` をセット）、`tone-json.html`（音色上書き JSON を編集）。
- ビブラート系は `delay-vibrato.html` / `delay-vibrato-demo.ts` で DelayVibrato オプションの有効・無効を試せるためカバーされている。
- ポルタメントを示す添付 JSON のプリセットや UI は見当たらず、Portamento オプションを試す専用デモは未実装。
- 音色レジスタへの SoftwareLfo を扱う添付 JSON のプリセットや UI もなく、SoftwareLfo を体験する専用デモは未実装。
